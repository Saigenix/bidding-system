# ============================================================================
# Bidding System SDK — Environment Variables
# ============================================================================
# Copy this file to .env and update values for your environment.
#   cp .env.example .env
# ============================================================================

# Server
SERVER_PORT=8080

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=bidding
DB_SSLMODE=disable
DB_MAX_CONNS=25
DB_MIN_CONNS=5

# JWT Authentication
JWT_SECRET=change-me-to-a-strong-random-secret
JWT_EXPIRATION_HOUR=24

# Logging (debug | info | warn | error)
LOG_LEVEL=info
